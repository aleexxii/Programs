#include<stdio.h>
int getArray();
void displayArray(int);

int main()

{
    c[i]=getArray();
    displayArray(c[i],a[i]);


    return 0;
}

int getArray() {
    int array[6],i;
    printf("Enter array");
        for ( i = 0; i < 6; i++)
        {
            scanf("%d",&array[6]);
        }

        return array[i];
        
}

void displayArray(int a[i]) {
    int a[6],i;
    for ( i = 0; i < 6; i++)
    {
        printf("%d",a[i]);
    }
    
}