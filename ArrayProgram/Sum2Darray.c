#include<stdio.h>

    void getArray();
    void addArray();

    int main () {



    }

    getArray() {
        int row,col,i;
            printf("Enter the column size and row size of array : ");
            scanf("%d\t%d",&row,&col);
        int a[row][col],b[row][col];

        printf("Enter first array values : ");
            for ( i = 0; i < col; i++ )
                {
                    for ( j = 0; j < row; j++)
                        scanf("%d",&a[row][col])
                }

    }

    addArray () {
        int sum;
        sum=a[row]+a[col];
    }